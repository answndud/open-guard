# test

Run tests, show failures, and propose minimal fix steps.

Steps:
- Run: `pnpm test`.
- If failures: summarize failing tests and the first error line.
- Propose the smallest fixes; avoid broad refactors.
